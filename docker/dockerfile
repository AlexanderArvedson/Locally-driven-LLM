FROM python:3.12-slim

WORKDIR /app

# git is required by GitPython for the pre-flight repo sync
RUN apt-get update && apt-get install -y --no-install-recommends git && rm -rf /var/lib/apt/lists/*

# trust all mounted directories — needed because the container runs as root
# while repo files are owned by the host user (git 2.35.2+ safe.directory check)
RUN git config --global --add safe.directory '*'

# install uv
RUN pip install --no-cache-dir uv

# copy dependency definitions first (for caching)
COPY pyproject.toml uv.lock* ./

# install dependencies from project definition into system site-packages
RUN uv pip install --system .

# copy application code
COPY . .

EXPOSE 8000

CMD ["python", "main.py"]