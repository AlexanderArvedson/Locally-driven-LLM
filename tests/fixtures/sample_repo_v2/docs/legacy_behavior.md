# Legacy Behavior

Older releases documented the following commands and behaviors:

- `python -m app.main --legacy-format`
- `python -m app.main --config configs/report.json`
- CSV export as the default report format
- `app/legacy/dead_adapter.py` as the supported compatibility layer

These notes are preserved for historical reference only.